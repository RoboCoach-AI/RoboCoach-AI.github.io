Run this from the repository directory (Python 3; no package installation needed):

```bash
python3 scripts/serve_local.py --port 17862
```

Open `http://127.0.0.1:17862/`, or jump directly to `http://127.0.0.1:17862/#trajectory-demo`.
